let count = 10; // ts infer that count is number because it sees 0. Explicity writing count as a number is not neccessary.
let name = "B";
let isAdmin = false;

console.log(count) 
console.log(name)
console.log(isAdmin)
